fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
Pillow==10.4.0
numpy==2.1.1

# Requires system package `libopenslide0` (Debian/Ubuntu) or `openslide` (brew/conda).
openslide-python==1.4.1

# For pyramidal TIFF / OME-TIFF writing. Real BigTIFF writer needs pyvips (system libvips).
tifffile==2024.9.20
imagecodecs==2024.9.22
