# Public URL of the histotopogram backend (see backend/ folder).
# Leave unset to run the frontend in browser-only mode; .mrxs uploads will show
# "Модуль .mrxs недоступен. Запустите backend-сервис."
VITE_BACKEND_URL=http://localhost:8000
